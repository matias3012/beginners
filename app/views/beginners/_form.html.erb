<%= form_with(model: beginner, local: true) do |form| %>
  <% if beginner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beginner.errors.count, "error") %> prohibited this beginner from being saved:</h2>

      <ul>
      <% beginner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :element %>
    <%= form.text_area :element %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
